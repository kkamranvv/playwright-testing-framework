// import { test, expect } from "../../../../fixtures/orangehrm/fixtures.js";
// import { Sidebar } from "../../../../pages/orangehrm/Sidebar/Sidebar.js";

// test("Active sidebar item is highlighted", async ({ login }) => {
//   const sidebar = new Sidebar(login);

//   await login.getByRole("link", { name: "PIM" }).click();

//   await expect(login).toHaveURL(/.*pim/);

//   await expect(sidebar.getMenuItem("PIM")).toHaveClass(/active/);

//   expect(await sidebar.isActive("PIM")).toBeTruthy();
// });

// import { test, expect } from "../../../../fixtures/orangehrm/fixtures.js";
// import { SearchMenu } from "../../../../pages/orangehrm/Search/SearchMenu.js";
// import { modules } from "../utils/data.js";

// for (const m of modules) {
//   test(`Search and navigate to ${m.name}`, async ({ login }) => {
//     const search = new SearchMenu(login);

//     await search.type(m.name);
//     await search.clickMenu(m.name);

//     await expect(login).toHaveURL(m.url);
//   });
// }
